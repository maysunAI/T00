# H23 — 会话结束与中断处理

> 适用：所有使用 T00/AI00 框架的用户

---

## What — 会话结束是什么

Claude Code 的一次"会话"从你打开终端开始，到你关闭或说「再见」结束。
会话内的所有上下文（你说了什么、AI 做了什么）只存在于当前内存，关闭后丢失。

T00 框架通过三个文件对抗这个问题：

| 文件 | 作用 | 谁读 |
|------|------|------|
| `RESUME.md` | 快速上下文（AI 下次自动读）| AI |
| `SESSION_LOG.md` | 完整会话记录（给人看）| 人 |
| `temp03_reply.md` | 当次问答详细内容 | 人 |

---

## Why — 为什么要做会话结束

- Claude Code **没有跨会话记忆**，每次重新打开都是全新的 AI
- 不存档 → 下次要重新解释背景 → 浪费大量时间
- 做了存档 → 下次说一句「请读取 RESUME.md」→ AI 立即恢复上下文

---

## When — 什么时候触发

| 情况 | 做法 |
|------|------|
| 正常结束 | 说「再见」|
| 临时中断（会回来）| 说「先停一下，帮我存档」|
| 意外关闭（忘了再见）| 下次开始说「请读取 RESUME.md」|
| 上下文快满（context warning）| 立刻说「再见」再开新会话 |

---

## Where — 存档文件在哪里

```
F:\T00\
├── RESUME.md                              ← AI 下次读这个
├── AI00_Common\_docs\discussion\
│   └── SESSION_LOG.md                     ← 人看的完整记录
└── temp03_reply.md                        ← 当次问答详细内容
```

---

## Who — 谁来做

说「再见」后，**AI 自动执行**，无需用户手动操作：
1. 追加 SESSION_LOG.md（做了什么 / 未完成）
2. 更新 RESUME.md（上次日期 / 3-5条要点 / 下一步 / 快速上下文）
3. 输出下次开始命令

用户只需要：
- 说「再见」
- 记得下次说「请读取 RESUME.md 继续上次的工作」

---

## How — 具体流程

### 正常结束（推荐）

```
你：再见
AI：（自动更新 SESSION_LOG.md + RESUME.md）
AI：请读取 RESUME.md 继续上次的工作。
    📖 本次会话记录：F:\T00\AI00_Common\_docs\discussion\SESSION_LOG.md
```

### 下次恢复

```
（新会话开始）
你：请读取 RESUME.md 继续上次的工作。
AI：（读取 RESUME.md）好的，上次做了 xxx，下一步是 xxx...
```

### 临时中断（只存档不结束）

```
你：帮我存档，待会回来
AI：（更新 RESUME.md）✅ 已存档，随时可以继续
```

### 紧急中断（直接关闭窗口）

下次开始时说：
```
请读取 RESUME.md 继续上次的工作。上次没有正常结束会话，从 RESUME.md 恢复即可。
```

---

## 注意事项

- **reply 超过 2000 行时**自动备份到 `Z01_archive/`，清空后继续（AI 会提示）
- **再见 = 最高权限**：AI 可以无需逐步确认，自动完成所有收尾工作
- 不要在重要会话进行到一半时让 context 超满（看右上角的 context 百分比）
- `temp03_reply.md` 是问答详细内容，不是 AI 记忆；真正的 AI 记忆在 RESUME.md

---

## 中断时的操作（含 clear / compact / reply清空）

### `/clear` — 清空当前上下文

```
（在 CLI 输入）
/clear
```

**作用**：清空 Claude Code 当前会话的 context，相当于重新开始。
**什么时候用**：context 快满、话题切换太多、想重新专注一个任务。
**注意**：不会影响文件，不会影响 RESUME.md，只清空 AI 的记忆。

---

### `/compact` — 压缩 context

```
（在 CLI 输入）
/compact
```

**作用**：把当前 context 内容压缩成摘要，腾出空间继续工作。
**什么时候用**：比 /clear 温和；想保留大致上下文，但 context 已经很长。

---

### reply 清空（人工操作）

reply 清空前必须先备份：
```
（告诉 AI）帮我备份 reply 并清空
```

AI 会：
1. 备份 reply 到 `Z01_archive/reply_YYYYMMDD-YYYYMMDD.md`
2. 清空 `temp03_reply.md`（保留标题行）
3. 保留最后一个 ══ 区块（下次会话直接看到上次结尾）

---

### 长时间不用（超过1周）

重新开始时说：
```
请读取 RESUME.md 继续上次的工作。已经有一段时间没用了，请帮我总结当前状态。
```

AI 会：
1. 读取 RESUME.md
2. 报告上次做了什么、下一步是什么
3. 确认项目状态（文件是否还在、任务是否还有效）

建议做一次 `/compact` 再继续。

---

## 快速参考

| 说什么 | AI 做什么 |
|--------|---------|
| 「再见」| 存 SESSION_LOG + 更新 RESUME + 输出恢复命令 |
| 「帮我存档」| 只更新 RESUME，不输出再见 |
| 「req 状态」| 报告 req/reply 当前行数 |
| 「清空沟通文件」| 备份 reply → 清空 3 个 temp 文件 |
